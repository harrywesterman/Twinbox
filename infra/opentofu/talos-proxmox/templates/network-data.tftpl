version: 2
ethernets:
  main:
    match:
      macaddress: ${mac}
    dhcp4: false
    addresses:
      - ${ip}/${prefix}
    routes:
      - to: 0.0.0.0/0
        via: ${gateway}
    nameservers:
      addresses:
%{ for server in dns_servers ~}
        - ${server}
%{ endfor ~}
