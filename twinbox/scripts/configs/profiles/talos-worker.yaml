# Talos Linux Worker Node Profile
# For Kubernetes worker nodes

vmid: 911
name: talos-worker-1
cores: 2
memory: 4096
disk: 20G
bridge: vmbr0
storage: local-lvm
iso_path: local:iso/talos-amd64.iso

# Talos-specific settings
node_type: worker
cluster_name: talos-cluster

# Optional: Additional settings
# vlan_id: 0