# config/

Pinned default versions and settings used across Twinbox provisioning scripts.

## Files

- `pinned-defaults.sh` — shell variables for tool and chart versions (Talos, OpenTofu, k9s, External Secrets, OpenBao) and Proxmox image defaults.
