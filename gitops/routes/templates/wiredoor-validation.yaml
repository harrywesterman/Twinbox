apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: wiredoor-validation
  namespace: default
spec:
  entryPoints:
    - webwiredoor
  routes:
    - kind: Rule
      match: PathPrefix(`/`)
      services:
        - kind: Service
          name: whoami
          port: 80