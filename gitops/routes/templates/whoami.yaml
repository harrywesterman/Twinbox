apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: whoami
  namespace: default
spec:
  entryPoints:
    - websecure
    - webwiredoor
  routes:
    - kind: Rule
      match: Host(`{{ .Values.hosts.whoami }}.{{ .Values.domain }}`)
      services:
        - kind: Service
          name: whoami
          port: 80
  tls: {}