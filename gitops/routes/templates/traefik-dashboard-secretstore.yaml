apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: traefik-dashboard-fields
  namespace: traefik
spec:
  provider:
    webhook:
      url: "http://bitwarden-cli.bitwarden.svc.cluster.local:8087/object/item/twinbox%2Fglobal%2Ftraefik-dashboard"
      result:
        jsonPath: "$.data.fields[?@.name==\"users\"].value"
