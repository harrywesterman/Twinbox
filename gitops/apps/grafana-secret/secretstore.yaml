apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: grafana-admin-fields
  namespace: monitoring
spec:
  provider:
    webhook:
      url: "http://bitwarden-cli.bitwarden.svc.cluster.local:8087/object/item/ddd2a44b-6f13-4c6f-84c0-e841edd35192"
      result:
        jsonPath: "$.data.login.{{ .remoteRef.property }}"
