apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: freshrss
  namespace: argocd
spec:
  project: default
  source:
    repoURL: __REPO_URL__
    targetRevision: __TARGET_REVISION__
    path: gitops/platform-apps/freshrss
    kustomize:
      patches:
        - target:
            kind: IngressRoute
            name: freshrss
          patch: |-
            - op: replace
              path: /spec/routes/0/match
              value: Host(`freshrss.__ZONE_NAME__`)
        - target:
            kind: IngressRoute
            name: freshrss-netbird
          patch: |-
            - op: replace
              path: /spec/routes/0/match
              value: Host(`freshrss.__ZONE_NAME__`)
  destination:
    server: https://kubernetes.default.svc
    namespace: freshrss
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
