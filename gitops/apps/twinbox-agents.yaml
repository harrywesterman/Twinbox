apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: twinbox-agents
  namespace: argocd
spec:
  project: default
  source:
    repoURL: __REPO_URL__
    targetRevision: __TARGET_REVISION__
    path: gitops/platform-apps/twinbox-agents
  destination:
    server: https://kubernetes.default.svc
    namespace: twinbox-agents
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
