apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: karakeep-set
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions:
    - missingkey=error
  generators:
    - clusters:
        selector:
          matchLabels:
            twinbox.io/domain-ready: "true"
            twinbox.io/app-karakeep: "enabled"
  template:
    metadata:
      name: karakeep
    spec:
      project: default
      sources:
        - repoURL: https://karakeep-app.github.io/helm-charts
          chart: karakeep
          targetRevision: "0.33.1"
          helm:
            values: |-
              applicationProtocol: https
              applicationHost: karakeep.{{index .metadata.annotations "twinbox.io/public-zone-name"}}
              secrets:
                karakeep:
                  enabled: false
                meilesearch:
                  enabled: false
              controllers:
                karakeep:
                  containers:
                    karakeep:
                      env:
                        DISABLE_PASSWORD_AUTH: "true"
                        OAUTH_ALLOW_DANGEROUS_EMAIL_ACCOUNT_LINKING: "true"
                        OAUTH_PROVIDER_NAME: Authentik
                        OAUTH_SCOPE: "openid email profile"
                        OAUTH_WELLKNOWN_URL: 'https://authentik.{{index .metadata.annotations "twinbox.io/public-zone-name"}}/application/o/karakeep/.well-known/openid-configuration'
                      resources:
                        requests:
                          cpu: 100m
                          memory: 512Mi
                        limits:
                          cpu: "1"
                          memory: 1Gi
                      probes:
                        liveness:
                          spec:
                            initialDelaySeconds: 10
                            timeoutSeconds: 5
                        readiness:
                          spec:
                            initialDelaySeconds: 10
                            timeoutSeconds: 5
                        startup:
                          enabled: true
                          custom: true
                          spec:
                            failureThreshold: 30
                            periodSeconds: 5
                            timeoutSeconds: 5
                            httpGet:
                              path: /api/health
                              port: 3000
                # Chrome stays within the cluster's baseline PodSecurity policy by
                # running without SYS_ADMIN and using the standard no-sandbox mode.
                chrome:
                  containers:
                    chrome:
                      image:
                        repository: docker.io/zenika/alpine-chrome
                        tag: "124"
                      args:
                        - --remote-debugging-address=0.0.0.0
                        - --remote-debugging-port=9222
                        - --headless
                        - --hide-scrollbars
                        - --disable-gpu
                        - --disable-dev-shm-usage
                        - --no-sandbox
                      securityContext:
                        allowPrivilegeEscalation: false
                        capabilities:
                          add: []
                        readOnlyRootFilesystem: true
                        runAsNonRoot: true
                        runAsUser: 1000
                        runAsGroup: 1000
                      resources:
                        requests:
                          cpu: 100m
                          memory: 100Mi
                        limits:
                          cpu: "1"
                          memory: 1Gi
        - repoURL: https://github.com/harrywesterman/Twinbox.git
          targetRevision: main
          path: gitops/platform-apps/karakeep
          kustomize:
            patches:
              - target:
                  kind: IngressRoute
                  name: karakeep
                patch: |-
                  - op: replace
                    path: /spec/routes/0/match
                    value: Host(`karakeep.{{index .metadata.annotations "twinbox.io/public-zone-name"}}`)
              - target:
                  kind: IngressRoute
                  name: karakeep-netbird
                patch: |-
                  - op: replace
                    path: /spec/routes/0/match
                    value: Host(`karakeep.{{index .metadata.annotations "twinbox.io/public-zone-name"}}`)
      destination:
        server: https://kubernetes.default.svc
        namespace: karakeep
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
