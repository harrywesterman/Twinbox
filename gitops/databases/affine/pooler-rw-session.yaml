apiVersion: postgresql.cnpg.io/v1
kind: Pooler
metadata:
  name: affine-db-pooler-rw-session
  namespace: databases
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  cluster:
    name: affine-db
  instances: 1
  type: rw
  pgbouncer:
    poolMode: session
    parameters:
      max_client_conn: "200"
      default_pool_size: "20"
