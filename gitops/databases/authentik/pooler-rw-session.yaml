apiVersion: postgresql.cnpg.io/v1
kind: Pooler
metadata:
  name: authentik-db-pooler-rw-session
  namespace: databases
spec:
  cluster:
    name: authentik-db
  instances: 2
  type: rw
  pgbouncer:
    poolMode: session
    parameters:
      max_client_conn: "200"
      default_pool_size: "20"
      min_pool_size: "5"
