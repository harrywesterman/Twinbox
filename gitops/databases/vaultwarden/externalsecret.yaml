apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: vaultwarden-db-credentials
  namespace: databases
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: openbao
    kind: ClusterSecretStore
  target:
    name: vaultwarden-db-credentials
    creationPolicy: Owner
    deletionPolicy: Delete
  data:
    - secretKey: username
      remoteRef:
        key: twinbox/global/vaultwarden
        property: VAULTWARDEN_POSTGRESQL__USERNAME
    - secretKey: password
      remoteRef:
        key: twinbox/global/vaultwarden
        property: VAULTWARDEN_POSTGRESQL__PASSWORD
