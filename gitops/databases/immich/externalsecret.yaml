apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: immich-db-credentials
  namespace: databases
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: openbao
    kind: ClusterSecretStore
  target:
    name: immich-db-credentials
    creationPolicy: Owner
    deletionPolicy: Delete
  data:
    - secretKey: username
      remoteRef:
        key: twinbox/global/immich
        property: IMMICH_POSTGRESQL__USERNAME
    - secretKey: password
      remoteRef:
        key: twinbox/global/immich
        property: IMMICH_POSTGRESQL__PASSWORD
