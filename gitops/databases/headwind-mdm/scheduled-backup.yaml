apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: headwind-mdm-db-backup
  namespace: databases
spec:
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
  schedule: "0 0 2 * * 2"
  backupOwnerReference: self
  cluster:
    name: headwind-mdm-db
