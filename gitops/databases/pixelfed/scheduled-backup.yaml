apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: pixelfed-db-backup
  namespace: databases
spec:
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
  schedule: "0 2 * * 3"
  backupOwnerReference: self
  cluster:
    name: pixelfed-db
