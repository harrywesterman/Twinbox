apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: coder-db
  namespace: databases
spec:
  instances: 1

  plugins:
    - name: barman-cloud.cloudnative-pg.io
      isWALArchiver: true
      parameters:
        barmanObjectName: coder-db-objectstore
        serverName: coder-db

  bootstrap:
    initdb:
      database: coder
      owner: coder
      secret:
        name: coder-db-credentials

  storage:
    size: 5Gi
    storageClass: longhorn-single

  resources:
    requests:
      cpu: 500m
      memory: 1Gi
    limits:
      cpu: "1"
      memory: 2Gi

  monitoring:
    enablePodMonitor: true
