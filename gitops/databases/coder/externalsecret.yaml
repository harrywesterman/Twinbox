apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: coder-db-credentials
  namespace: databases
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: openbao
    kind: ClusterSecretStore
  target:
    name: coder-db-credentials
    creationPolicy: Owner
    deletionPolicy: Delete
  data:
    - secretKey: username
      remoteRef:
        key: twinbox/global/coder
        property: CODER_POSTGRESQL__USERNAME
    - secretKey: password
      remoteRef:
        key: twinbox/global/coder
        property: CODER_POSTGRESQL__PASSWORD
