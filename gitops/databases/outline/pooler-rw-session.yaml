apiVersion: postgresql.cnpg.io/v1
kind: Pooler
metadata:
  name: outline-db-pooler-rw-session
  namespace: databases
spec:
  cluster:
    name: outline-db
  instances: 1
  type: rw
  pgbouncer:
    poolMode: session
    parameters:
      max_client_conn: "200"
      default_pool_size: "20"
