apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: outline-db-credentials
  namespace: databases
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: openbao
    kind: ClusterSecretStore
  target:
    name: outline-db-credentials
    creationPolicy: Owner
    deletionPolicy: Delete
  data:
    - secretKey: username
      remoteRef:
        key: twinbox/global/outline
        property: OUTLINE_POSTGRESQL__USERNAME
    - secretKey: password
      remoteRef:
        key: twinbox/global/outline
        property: OUTLINE_POSTGRESQL__PASSWORD
