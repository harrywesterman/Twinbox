apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: CHANGEME-db-backup
  namespace: databases
spec:
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
  schedule: "0 0 2 * * 7"
  backupOwnerReference: self
  cluster:
    name: CHANGEME-db
