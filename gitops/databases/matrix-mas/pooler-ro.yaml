apiVersion: postgresql.cnpg.io/v1
kind: Pooler
metadata:
  name: matrix-mas-db-pooler-ro
  namespace: databases
spec:
  cluster:
    name: matrix-mas-db
  instances: 2
  type: ro
  pgbouncer:
    poolMode: transaction
    parameters:
      max_client_conn: "100"
      default_pool_size: "10"
