apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: mastodon-db-credentials
  namespace: databases
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: openbao
    kind: ClusterSecretStore
  target:
    name: mastodon-db-credentials
    creationPolicy: Owner
    deletionPolicy: Delete
  data:
    - secretKey: username
      remoteRef:
        key: twinbox/global/mastodon
        property: MASTODON_POSTGRESQL__USERNAME
    - secretKey: password
      remoteRef:
        key: twinbox/global/mastodon
        property: MASTODON_POSTGRESQL__PASSWORD
