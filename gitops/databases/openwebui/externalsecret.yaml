apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: openwebui-db-credentials
  namespace: databases
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: openbao
    kind: ClusterSecretStore
  target:
    name: openwebui-db-credentials
    creationPolicy: Owner
    deletionPolicy: Delete
  data:
    - secretKey: username
      remoteRef:
        key: twinbox/global/openwebui
        property: OPENWEBUI_POSTGRESQL__USERNAME
    - secretKey: password
      remoteRef:
        key: twinbox/global/openwebui
        property: OPENWEBUI_POSTGRESQL__PASSWORD
