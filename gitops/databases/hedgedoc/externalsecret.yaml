apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: hedgedoc-db-credentials
  namespace: databases
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: openbao
    kind: ClusterSecretStore
  target:
    name: hedgedoc-db-credentials
    creationPolicy: Owner
    deletionPolicy: Delete
  data:
    - secretKey: username
      remoteRef:
        key: twinbox/global/hedgedoc
        property: HEDGEDOC_POSTGRESQL__USERNAME
    - secretKey: password
      remoteRef:
        key: twinbox/global/hedgedoc
        property: HEDGEDOC_POSTGRESQL__PASSWORD
