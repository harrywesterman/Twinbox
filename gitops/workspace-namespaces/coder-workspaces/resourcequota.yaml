apiVersion: v1
kind: ResourceQuota
metadata:
  name: coder-workspaces-quota
  namespace: coder-workspaces
spec:
  hard:
    pods: "10"
    requests.cpu: "8"
    requests.memory: "16Gi"
    limits.cpu: "16"
    limits.memory: "32Gi"
    persistentvolumeclaims: "10"
    services: "5"
