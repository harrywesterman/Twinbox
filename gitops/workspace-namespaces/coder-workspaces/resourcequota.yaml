apiVersion: v1
kind: ResourceQuota
metadata:
  name: coder-workspaces-quota
  namespace: coder-workspaces
spec:
  hard:
    pods: "10"
    requests.cpu: "16"
    requests.memory: "64Gi"
    limits.cpu: "32"
    limits.memory: "128Gi"
    persistentvolumeclaims: "10"
    services: "5"
