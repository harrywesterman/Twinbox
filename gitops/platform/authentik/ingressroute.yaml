apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: authentik
  namespace: authentik
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`authentik.bierineenweek.nl`)
      services:
        - kind: Service
          name: authentik-server
          port: 80
  tls: {}
