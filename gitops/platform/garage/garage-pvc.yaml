apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: garage-data
  namespace: velero
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 20Gi
