apiVersion: v1
kind: Service
metadata:
  name: nextcloud-eurooffice
  namespace: nextcloud
  labels:
    app.kubernetes.io/name: nextcloud-eurooffice
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: nextcloud-eurooffice
  ports:
    - name: http
      port: 80
      targetPort: http
