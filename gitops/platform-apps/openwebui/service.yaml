apiVersion: v1
kind: Service
metadata:
  name: openwebui
  namespace: openwebui
  labels:
    app.kubernetes.io/name: openwebui
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: openwebui
  ports:
    - name: http
      port: 80
      targetPort: http
