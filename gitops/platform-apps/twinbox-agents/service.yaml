apiVersion: v1
kind: Service
metadata:
  name: twinbox-agents
  namespace: twinbox-agents
spec:
  selector:
    app.kubernetes.io/name: twinbox-agents
  ports:
    - name: http
      port: 80
      targetPort: http
