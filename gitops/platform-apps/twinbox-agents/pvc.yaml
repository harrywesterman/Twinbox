apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: twinbox-agents-data
  namespace: twinbox-agents
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 5Gi
