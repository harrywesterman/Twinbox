apiVersion: v1
kind: Service
metadata:
  name: outline
  namespace: outline
  labels:
    app.kubernetes.io/name: outline
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: outline
  ports:
    - name: http
      port: 80
      targetPort: http
