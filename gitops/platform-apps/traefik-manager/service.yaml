apiVersion: v1
kind: Service
metadata:
  name: traefik-manager
  namespace: traefik-manager
  labels:
    app.kubernetes.io/name: traefik-manager
spec:
  selector:
    app.kubernetes.io/name: traefik-manager
  ports:
    - name: http
      port: 80
      targetPort: 5000
  type: ClusterIP
