apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: coder
  namespace: coder
spec:
  entryPoints:
    - websecure
  routes:
    - kind: Rule
      match: Host(`coder.__ZONE_NAME__`)
      services:
        - kind: Service
          name: coder
          port: 80
  tls: {}
