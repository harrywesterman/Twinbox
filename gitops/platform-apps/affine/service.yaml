apiVersion: v1
kind: Service
metadata:
  name: affine
  namespace: affine
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
  labels:
    app.kubernetes.io/name: affine
spec:
  selector:
    app.kubernetes.io/name: affine
  ports:
    - name: http
      port: 3010
      targetPort: http
