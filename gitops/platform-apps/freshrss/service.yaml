apiVersion: v1
kind: Service
metadata:
  name: freshrss
  namespace: freshrss
  labels:
    app.kubernetes.io/name: freshrss
spec:
  selector:
    app.kubernetes.io/name: freshrss
  ports:
    - name: http
      port: 80
      targetPort: http
  type: ClusterIP
